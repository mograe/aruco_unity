{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Unity Editor (native)",
      "type": "cppvsdbg",
      "request": "attach",
      "processId": "${command:pickProcess}"
    }
  ]
}
